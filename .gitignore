# Raw & processed data — ignore the files, keep the folders via .gitkeep
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep

# Exported model weights — tracked via Drive / DVC separately
models/*
!models/.gitkeep
models/lstm_model.pth

# Local environment
.env
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Jupyter checkpoints
.ipynb_checkpoints/

# Phase 2 simulation outputs — regenerated by run_dry_run.py / consumer_engine.py / visualize_results.py
simulation/output/*
!simulation/output/.gitkeep

# OS
.DS_Store
Thumbs.db
